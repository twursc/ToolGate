# Server
PORT=3000
MCP_PORT=3001

# Admin
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme
SESSION_SECRET=mcai-mcp-gateway-secret-change-me

# Storage: "sqlite" or "postgres"
STORAGE_TYPE=sqlite

# Logging: "error" | "warn" | "info" | "debug"
LOGGING=info

# Set to any non-empty value to skip MCP API key authentication
# MCP_GW_AUTH_FREE=1
